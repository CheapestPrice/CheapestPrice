'use strict';

describe('myApp.vistaRegistro module', function() {

  beforeEach(module('myApp.vistaRegistro'));

  describe('vistaRegistro controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var vistaRegistro = $controller('vistaRegistro');
      expect(vistaRegistro).toBeDefined();
    }));

  });
});