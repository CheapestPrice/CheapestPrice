'use strict';

describe('myApp.vistaPrincipal module', function() {

  beforeEach(module('myApp.vistaPrincipal'));

  describe('vistaPrincipal controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var vistaPrincipalCtrl = $controller('vistaPrincipalCtrl');
      expect(vistaPrincipalCtrl).toBeDefined();
    }));

  });
});