'use strict';

describe('myApp.viewItem module', function() {

  beforeEach(module('myApp.view1'));

  describe('viewItem controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewItemCtrl = $controller('ViewItemCtrl');
      expect(viewItemCtrl).toBeDefined();
    }));

  });
});