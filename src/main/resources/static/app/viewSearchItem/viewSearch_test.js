'use strict';

describe('myApp.viewSearch module', function() {

  beforeEach(module('myApp.viewSearch'));

  describe('viewSearch controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewItemCtrl = $controller('ViewSearchCtrl');
      expect(viewItemCtrl).toBeDefined();
    }));

  });
});