'use strict';

describe('myApp.viewAddProducts module', function() {

  beforeEach(module('myApp.view1'));

  describe('viewAddProducts controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewItemCtrl = $controller('ViewAddProductsCtrl');
      expect(viewItemCtrl).toBeDefined();
    }));

  });
});