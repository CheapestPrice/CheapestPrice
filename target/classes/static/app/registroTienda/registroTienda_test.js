'use strict';

describe('myApp.registroTienda module', function() {

  beforeEach(module('myApp.registroTienda'));

  describe('registroTienda controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var registroTienda = $controller('registroTienda');
      expect(registroTienda).toBeDefined();
    }));

  });
});